server:
  port: 9001

spring:
  application:
    name: task-service

  datasource:
    username: ${DATABASE_USER:postgres}
    password: ${DATABASE_PASSWORD:postgres}
    url: jdbc:postgresql://${DATABASE_URL:localhost:5432}/tasktracker
    driver-class-name: org.postgresql.Driver
    hikari:
      schema: task-service
      connection-timeout: 5000

  jpa:
    open-in-view: false